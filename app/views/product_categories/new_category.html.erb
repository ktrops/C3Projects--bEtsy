<% unless flash[:errors].nil? %>
  <%= render partial: "shared/errors", locals: {instance: @category} %>
<% end %>

<h1>Add a New Category</h1>

<%= form_for @category, url: category_path, method: :post do |f| %>

  <%= f.label :name %>
  <%= f.text_area :name %>

  <%= f.submit %>

<% end %>